<Window x:Class="InstallerClean.OrphanedFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:InstallerClean.Resources"
        xmlns:a11y="clr-namespace:InstallerClean.Helpers"
        xmlns:chrome="clr-namespace:InstallerClean.Controls"
        Title="{loc:Translate Window.Orphaned.Title}"
        Width="920" Height="540"
        MinWidth="600" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Surface.Sidebar}"
        Loaded="Window_Loaded">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" GlassFrameThickness="-1"
                      ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="{StaticResource DetailWindowMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    VerticalAlignment="Center" Margin="-8,-4,0,0">
            <chrome:DecorativeImage Source="Assets/splash-icon.png" Width="16" Height="16"
                   RenderOptions.BitmapScalingMode="HighQuality" Margin="{StaticResource GapRight.Sm}"/>
            <TextBlock Text="{loc:Translate Window.Orphaned.Title}" Foreground="{StaticResource Text.Body}"
                       FontSize="{DynamicResource Type.Heading.Sm}" FontWeight="Medium"/>
        </StackPanel>
        <Button Grid.Row="0" Style="{StaticResource CaptionCloseButton}" Content="&#xE8BB;"
                Click="CloseClick" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="{StaticResource CloseButtonOverhangMargin}" AutomationProperties.Name="{loc:Translate Automation.CloseWindow}" ToolTip="{loc:Translate Tooltip.Close}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <!-- Auto plus an explicit text-scaled width on the details
                     card itself: ColumnDefinition.Width is a GridLength,
                     which the double-typed TextScaled binding cannot
                     target. -->
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardNoPad}" ClipToBounds="True">
                <!-- ListView+GridView pattern matching
                     RegisteredFilesWindow. The GridView column headers
                     let screen readers announce each row as column-
                     headed cells ("FileName, Reason, Size"); a flat
                     ListBox row announces as a single cell with the
                     values run together. -->
                <!-- Negative top/bottom margin bleeds the ListView past the
                     CardNoPad inner edges so the first and last rows sit flush
                     with the card border instead of inside a thin inset. The
                     Orphaned and Registered detail windows share this 0,-1,0,-6
                     value as a pair. -->
                <ListView x:Name="FilesList" ItemsSource="{Binding Files}"
                          SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                          BorderThickness="0" Background="Transparent"
                          Padding="0" Margin="0,-1,0,-6"
                          GridViewColumnHeader.Click="ColumnHeader_Click"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <!-- Spoken row name from the visible cells. Without the
                         override, the item automation peer falls back to the
                         bound record's generated ToString and a screen reader
                         reads the whole member dump per row. -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                            <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <!-- The three column widths total under the list
                                 pane's ~466 units at the 920 default (872
                                 content minus the 12 gap, the 380 details
                                 pane, the card border and the scrollbar),
                                 and they scale with the OS text size so the
                                 names and sizes stay whole as they grow.
                                 Horizontal scrolling is Auto, not Disabled:
                                 when the scaled columns outgrow the pane (a
                                 large scale on a small screen, or a
                                 user-shrunk window) a scrollbar appears
                                 rather than the rightmost columns sitting
                                 unreachable past the edge; at 100% in the
                                 default window no scrollbar shows.
                                 Installer-cache names are short random hex
                                 (8.3 style), so 270 shows them whole. -->
                            <GridViewColumn Header="{loc:Translate Field.File}" Width="{a11y:TextScaled 270}"
                                            x:Name="ColFileName">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FileName}" Foreground="{StaticResource Text.Heading}"
                                                   FontSize="{DynamicResource Type.Heading.Sm}" TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding FullPath}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{loc:Translate Field.Reason}" Width="{a11y:TextScaled 100}"
                                            x:Name="ColReason">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Reason}"
                                                   FontSize="{DynamicResource Type.Caption}" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <!-- Text.Muted on the selected-row background
                                                     (Action.Primary.Background) is 4.04:1 (fails AA
                                                     for normal text at 11pt). Lift to Text.Body on
                                                     selection so the load-bearing Orphaned-vs-Superseded
                                                     distinction stays legible on the row a low-vision
                                                     user has just navigated to. -->
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource Text.Muted}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Text.Body}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{loc:Translate Field.Size}" Width="{a11y:TextScaled 90}"
                                            x:Name="ColSize">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SizeDisplay}" Foreground="{StaticResource Text.Body}"
                                                   FontSize="{DynamicResource Type.Body}" TextAlignment="Right"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}" Width="{a11y:TextScaled 380}">
                <Grid>
                    <TextBlock Text="{loc:Translate Body.NoFileSelected}"
                               Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Body}" FontStyle="Italic"
                               VerticalAlignment="Center" HorizontalAlignment="Center"
                               TextAlignment="Center" TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasSelection}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Text="{loc:Translate Body.NoMetadata}"
                               Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Body}" FontStyle="Italic"
                               VerticalAlignment="Center" HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Visibility="{Binding ShowNoMetadata, Converter={StaticResource BoolToVis}}"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Visibility="{Binding ShowDetails, Converter={StaticResource BoolToVis}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="8"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="{loc:Translate Field.Reason}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding SelectedFile.Reason, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}"
                                     AutomationProperties.Name="{loc:Translate Field.Reason}"/>
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="{loc:Translate Field.Author}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding SelectedDetails.Author, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Author}"/>
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="{loc:Translate Field.Application}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding SelectedDetails.AppName, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Application}"/>
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="{loc:Translate Field.Title}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding SelectedDetails.Title, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Title}"/>
                            <TextBlock Grid.Row="8" Grid.Column="0" Text="{loc:Translate Field.Subject}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="8" Grid.Column="2" Text="{Binding SelectedDetails.Subject, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Subject}"/>
                            <TextBlock Grid.Row="10" Grid.Column="0" Text="{loc:Translate Field.Keywords}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="10" Grid.Column="2" Text="{Binding SelectedDetails.Keywords, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Keywords}"/>
                            <TextBlock Grid.Row="12" Grid.Column="0" Text="{loc:Translate Field.SigningCertificate}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"
                                       ToolTip="{loc:Translate Tooltip.SigningCertificate}"/>
                            <TextBox Grid.Row="12" Grid.Column="2" Text="{Binding SelectedDetails.DigitalSignature, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.SigningCertificate}"/>
                            <TextBlock Grid.Row="14" Grid.Column="0" Text="{loc:Translate Field.FileSize}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="14" Grid.Column="2" Text="{Binding SelectedFile.SizeDisplay, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}"
                                     AutomationProperties.Name="{loc:Translate Field.FileSize}"/>
                            <TextBlock Grid.Row="16" Grid.Column="0" Text="{loc:Translate Field.Comment}" Foreground="{StaticResource Text.Dim}" FontSize="{DynamicResource Type.Caption}"/>
                            <TextBox Grid.Row="16" Grid.Column="2" Text="{Binding SelectedDetails.Comments, Mode=OneWay}" Style="{StaticResource SelectableText}" Foreground="{StaticResource Text.Heading}" FontSize="{DynamicResource Type.Body}" TextWrapping="Wrap"
                                     AutomationProperties.Name="{loc:Translate Field.Comment}"/>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <DockPanel Grid.Row="3" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" Text="{Binding Summary}"
                       Foreground="{StaticResource Text.Body}" FontSize="{DynamicResource Type.Body}" VerticalAlignment="Center"/>
            <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryPill}"
                    Content="{loc:Translate Action.Close}" IsCancel="True"/>
        </DockPanel>
    </Grid>
</Window>
